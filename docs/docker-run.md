# Docker run guide

Run the project from the repository root:

```bash
docker compose up --build
```

After the container starts, open:

```text
http://localhost:8000/
```

Stop the running project with `Ctrl+C`, or use `docker compose down` if it was started in detached mode.
